# Optional: map coauthor last names to alternate first-name spellings and a URL,
# used to link coauthor names in the publications list. The site owner's own
# name (Tianyi Wang) is bolded automatically via first_name/last_name in _config.yml.
